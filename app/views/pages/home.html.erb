<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url('<%= asset_path('landing.jpg') %>'); background-size: cover; background-position: center; min-height: 100vh; display: flex; align-items: center;">
  <div class="container py-5 text-white">
    <div class="row">
      <div class="col-md-6">
        <h1 class="display-4 fw-bold">Welcome to AirCnC</h1>
        <p class="lead">Rent countries like never before. A global booking experience with a local touch.</p>

        <% if user_signed_in? %>
          <p>Welcome back, <%= current_user.first_name %>!</p>
        <% end %>

        <%= link_to "Browse all countries", countries_path, class: "btn btn-outline-light mt-4" %>
      </div>
      <!-- Removed the image column since background image is used -->
    </div>
  </div>
</div>
