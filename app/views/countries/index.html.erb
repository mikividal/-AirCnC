<nav class="navbar navbar-expand-lg navbar-lewagon border-bottom shadow-sm sticky-top mb-4">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
      <strong>AirCnC</strong>
    <% end %>
  </div>
</nav>

<div class="container mt-5">
  <h1 class="mb-4">Countries</h1>

  <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4">
    <% @countries.each do |country| %>
      <div class="col">
        <div class="card h-100 shadow-sm">
          <div class="card-body text-center">
            <h5 class="card-title">
              <%= link_to country.name, country_path(country), class: "text-decoration-none stretched-link" %>
            </h5>
            <p class="card-text text-muted"><%= country.tag_line %></p>
            <p class="fw-bold">£<%= number_to_currency(country.price, unit: "", precision: 2) %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
